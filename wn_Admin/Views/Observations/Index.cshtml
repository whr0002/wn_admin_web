@model IEnumerable<wn_Admin.Models.Safety.Observation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ObservationName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ObservationName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ObservationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ObservationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ObservationID })
        </td>
    </tr>
}

</table>
