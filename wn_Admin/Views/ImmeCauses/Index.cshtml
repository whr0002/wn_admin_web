@model IEnumerable<wn_Admin.Models.Safety.ImmeCause>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ImmeCauseName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ImmeCauseName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ImmeCauseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ImmeCauseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ImmeCauseID })
        </td>
    </tr>
}

</table>
