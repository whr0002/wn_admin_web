@model IEnumerable<wn_Admin.Models.Safety.KeyState>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.KeyStateName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.KeyStateName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.KeyStateID }) |
            @Html.ActionLink("Details", "Details", new { id=item.KeyStateID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.KeyStateID })
        </td>
    </tr>
}

</table>
