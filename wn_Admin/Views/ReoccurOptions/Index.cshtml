@model IEnumerable<wn_Admin.Models.Safety.ReoccurOption>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ReoccurOptionName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ReoccurOptionName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ReoccurOptionID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ReoccurOptionID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ReoccurOptionID })
        </td>
    </tr>
}

</table>
