@model IEnumerable<wn_Admin.Models.CompanyModels.EmployeeRole>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Employee.FirstMidName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoleID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Employee.FirstMidName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoleID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EmployeeRoleID }) |
            @Html.ActionLink("Details", "Details", new { id=item.EmployeeRoleID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EmployeeRoleID })
        </td>
    </tr>
}

</table>
