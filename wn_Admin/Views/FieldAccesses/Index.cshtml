@model IEnumerable<wn_Admin.Models.CModels.FieldAccess>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FieldAccessName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FieldAccessName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FieldAccessID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FieldAccessID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FieldAccessID })
        </td>
    </tr>
}

</table>
