@model IEnumerable<wn_Admin.Models.Safety.AccidentType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MajorAccidentForm.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AccidentTypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MajorAccidentForm.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AccidentTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AccidentTypeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AccidentTypeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AccidentTypeID })
        </td>
    </tr>
}

</table>
